# --- Single Model Mode (Basic/VLM Mode) ---
# If LAYOUT_MODEL is empty, the agent will use this configuration.
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o

# --- Hybrid Mode Configuration (Layout + Reasoner) ---
# Enable this by filling BOTH variables below. If set, these override the basic mode.
# --- Layout Extraction Model (e.g. ShowUI) ---
LAYOUT_BASE_URL=http://localhost:8005/v1
LAYOUT_API_KEY=any_value
LAYOUT_MODEL=showlab/ShowUI-2B

# --- Reasoning Model (e.g. Ernie, GPT-4o) ---
REASONER_BASE_URL=https://api.openai.com/v1
REASONER_API_KEY=your_api_key_here
REASONER_MODEL=gpt-4o
# --------------------------------------------

# Camera Source Configuration
# 0/1: Local WebCam | http://...:5001/stream: Remote Stream
CAMERA_SOURCE=0
# Camera Resolution: 1080p, 720p, 480p
CAMERA_RESOLUTION=720p

# HID Configuration
HID_HOST=192.168.2.239
HID_PORT=80
